// TODO Phase 3: GET /salons/{salonId}/slots, POST .../bookings,
// DELETE .../bookings/{id}, GET my-bookings + history.
// All slot-lock / window / overlap checks are server-side (Cloud Functions) —
// this file only calls those functions, never re-implements the rules.
export {};
